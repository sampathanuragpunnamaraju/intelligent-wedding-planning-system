/**
 * Shared API DTOs used to keep successful and failed responses consistent.
 *
 * <p>Feature controllers should return {@link com.eventmanagement.dto.api.ApiResponse}
 * envelopes and expose DTOs rather than persistence entities.</p>
 */
package com.eventmanagement.dto.api;
